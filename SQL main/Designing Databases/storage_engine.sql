-- Storage Engines

-- Showing all available engines
SHOW ENGINES;

-- InnoDB is default MySQL database engine which support many features like; transaction, views and procedures
-- We shouldn't have to change database engine unless we have to work with other older engines specifically